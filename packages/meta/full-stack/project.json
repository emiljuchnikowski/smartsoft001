{
  "name": "full-stack",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "tags": ["scope:meta"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "cache": true,
      "inputs": ["{projectRoot}/package.json", "{projectRoot}/README.md"],
      "options": {
        "commands": [
          "mkdir -p dist/packages/meta/full-stack",
          "cp packages/meta/full-stack/package.json packages/meta/full-stack/README.md dist/packages/meta/full-stack/"
        ],
        "parallel": false
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    }
  }
}
