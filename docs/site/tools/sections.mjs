/**
 * Fixed order of the documentation sections. Every `page.md` declares one of
 * these in its frontmatter (`section:`); the navigation generator and
 * `docs-check` both read this list, so it lives in one place.
 */
export const SECTION_ORDER = [
  'Getting started',
  'Packages',
  'Components',
  'CRUD',
  'Skills',
  'Contributing',
]
