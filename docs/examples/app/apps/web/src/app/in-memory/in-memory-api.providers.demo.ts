import { Provider } from '@angular/core';

import { IN_MEMORY_API_PROVIDER } from './in-memory-api.interceptor';

/** The `demo` build's replacement of `in-memory-api.providers.ts`. */
export const IN_MEMORY_API_PROVIDERS: Provider[] = [IN_MEMORY_API_PROVIDER];
